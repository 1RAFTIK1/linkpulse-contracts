# gen/ намеренно НЕ игнорируем: сгенерированный код коммитим, чтобы сервисы
# подключали модуль обычным `go get` без установленного buf на их стороне.

# Локальные артефакты
*.log
.DS_Store

# Бинарники инструментов, если кто-то ставит их в ./bin
/bin/
